<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberRegistration.datasource.MemberMapper">
    <resultMap id="member" type="memberRegistration.model.member.Member" autoMapping="true">
    </resultMap>

    <insert id="save">
        insert into myschema.member values (#{id}, #{name})
    </insert>

</mapper>
